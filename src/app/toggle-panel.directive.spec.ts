import { TogglePanelDirective } from './toggle-panel.directive';

describe('TogglePanelDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglePanelDirective();
    expect(directive).toBeTruthy();
  });
});
