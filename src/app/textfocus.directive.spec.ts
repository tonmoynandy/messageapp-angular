import { TextfocusDirective } from './textfocus.directive';

describe('TextfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new TextfocusDirective();
    expect(directive).toBeTruthy();
  });
});
